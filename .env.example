# Connection string from the Postgres project you already have
# (Neon, Supabase, or Vercel Postgres). Do not commit the real value.
# Vercel also injects this as POSTGRES_URL when the database is linked there.
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
